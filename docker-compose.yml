services:
  api:
    build: .
    depends_on:
      - postgres
    ports:
      - "8000:8000"

    env_file:
      - .env

  postgres:
    image: postgres
    env_file:
      - .env
    volumes:
      - postify-db:/var/lib/postgresql

volumes:
  postify-db: